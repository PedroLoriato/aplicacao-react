import appEstilos from "../../App.module.css";
import estilos from "./SobreMim.module.css";

function SobreMim() {
    return (
        <main className={`${appEstilos.DfColCenter} ${estilos.SobreMim}`}>
            <h1>OI</h1>
        </main>
    );
}

export default SobreMim;